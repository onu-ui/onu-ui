import type { CustomShortcut } from '../types'

export const skeletons: CustomShortcut[] = [
  ['skeleton', 'animate-pulse motion-reduce:animate-none o-radius-md bg-accent'],
  ['skeleton-text', 'skeleton h-4 w-full'],
  ['skeleton-avatar', 'skeleton size-10 rounded-full'],
]
