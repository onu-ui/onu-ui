import type { CustomShortcut } from '../types'

export const separators: CustomShortcut[] = [
  ['separator', 'h-px w-full shrink-0 bg-border'],
  ['separator-v', 'h-full w-px shrink-0 bg-border'],
  ['separator-strong', 'bg-foreground/20'],
]
